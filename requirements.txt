# Core web framework
Flask==3.1.3

# Numerical / scientific
numpy==1.26.4
scipy==1.13.1
pandas==2.3.3

# Image processing & computer vision
# Headless OpenCV: the app is a server-side image pipeline with NO GUI calls
# (no cv2.imshow/namedWindow/waitKey anywhere), so the non-GUI wheel is the
# correct distribution and avoids the unused Qt/GTK GUI dependencies. Pinned to
# 4.10.0.84, whose wheels declare numpy 1.x compatibility. The previously pinned
# opencv-python==4.13.0.92 required a numpy version incompatible with the pinned
# numpy==1.26.4 / TensorFlow 2.16.2 stack on Python 3.9, which made
# `pip install -r requirements.txt` unresolvable (verified by dry-run). This is
# the SAME numerical OpenCV version used by requirements-ci.txt.
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
pillow==11.3.0
# Steerable pyramid for the subband-entropy visual-complexity metric
# (hard import in stage1/visual_complexity.py). Required for Stage 1.
pyrtools==1.0.10

# Machine learning
scikit-learn==1.6.1

# Model interpretation (used by scripts/feature_ablation_shap.py)
shap==0.49.1

# Deep learning (saliency model — UMSI++)
# On Apple Silicon, tensorflow-macos is installed automatically as a dependency.
tensorflow==2.16.2
keras==3.10.0

# Optional OCR for text reading-cost analysis (cognitive/text_reader.py).
# Pulls in torch. IMPORTANT: keep numpy pinned to 1.26.4 above — newer easyocr
# stacks try to upgrade numpy to >=2.0, which breaks TensorFlow 2.16.2. The
# feature degrades gracefully if this package is absent.
easyocr==1.7.2

# Plotting (optional, used by analysis scripts)
matplotlib==3.9.4

# Testing (the README documents `python3 -m pytest tests/`)
pytest==8.4.2
