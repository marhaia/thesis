# Lightweight dependency set for CI smoke/regression tests (tests/test_smoke.py).
# Deliberately EXCLUDES the heavy ML stack (tensorflow, torch, easyocr, shap):
# the saliency stage degrades gracefully to image-only features, so the tested
# endpoints run without it. Keeping CI light makes it fast and avoids pulling
# multi-hundred-MB wheels on every push.
#
# The full, pinned runtime stack lives in requirements.txt; a separate CI job
# dry-run-resolves that file to catch real dependency conflicts.

Flask==3.1.3
numpy==1.26.4
scipy==1.13.1
# Headless OpenCV: CI runners have no display server. 4.10 supports numpy 1.x.
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
pillow==11.3.0
pyrtools==1.0.10
pytest==8.4.2
